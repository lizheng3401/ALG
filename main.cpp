#include <iostream>
#include <cstdio>
#include <iomanip>
#include <cstring>

using namespace std;

void add(char x[], char y[], int lx, int ly)
{
    int num[1000];
    int X[500] = {0}, Y[500] = {0};
    for (int i = 0; i < length; ++i) {

    }
}
int main()
{
    char x[500], y[500];
    cin>>x>>y;
    int xx =
    if(strlen(x) > strlen(y))
    {
        add(x, y);
    }else{
        add(y,x);
    }
    return 0;
}